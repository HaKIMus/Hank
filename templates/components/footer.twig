<nav class="navbar navbar-expand-lg navbar-hank">
    <div class="row d-flex-inline">
        <a class="navbar-brand" href="{{ path('hank_homepage') }}"><h1>Hank</h1></a>

        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="nav-link {% if app.request.get('_route') == 'hank_homepage' %}nav-link-active{% endif %}"
                   href="{{ path('hank_homepage') }}">
                    Home
                </a>
            </li>

            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <a class="nav-link {% if app.request.get('_route') == 'hank_client_panel' %}nav-link-active{% endif %}"
                       href="{{ path('hank_client_panel') }}">
                        Client Panel
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('logout') }}">
                        Sign Out
                    </a>
                </li>

                {% elseif not is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.get('_route') == 'hank_sign_in' %}nav-link-active{% endif %}"
                           href="{{ path('hank_sign_in') }}">
                            Sign In
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link {% if app.request.get('_route') == 'hank_sign_up' %}nav-link-active{% endif %}"
                           href="{{ path('hank_sign_up') }}">
                            Sign Up
                        </a>
                    </li>
            {% endif %}
        </ul>
    </div>
</nav>
